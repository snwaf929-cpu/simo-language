# Simo VS Code support

This directory contains the source for a minimal VS Code language extension.

For local testing, open this directory in VS Code and press `F5`. To package it, install `@vscode/vsce` and run `vsce package` from this directory.
